kind: Deployment
apiVersion: apps/v1

metadata:
  name: mysql
  labels:
    app: mysql
    project: expense
    environment: development
  namespace: ex-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mysql
      project: expense
      environment: development
  template:
    metadata:
      labels:
        app: mysql
        project: expense
        environment: development
    spec:
      containers:
      - name: mysql
        image: joindevops/mysql:v1
        
