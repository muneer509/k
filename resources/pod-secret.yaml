apiVersion: v1
kind: Pod
metadata:
    name: pod-secret
spec:
    containers:
    - name: mycontainer
      image: nginx:latest
      envFrom:
        - secretRef:
            name: my-secret
            
