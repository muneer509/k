kind: Pod
apiVersion: v1
metadata:
  name: tolaration
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: hardware
    operator: Equal
    value: gpu
    effect: NoSchedule
